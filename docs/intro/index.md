# 介绍
该项目是基于 `vite2` + `vue3` + `typescript` + `element-plus` 二次封装的组件库，提供了一下组件：

- 省市区选择器
- 时间选择器
- 日期选择器
- 城市选择器
- 趋势标记
- 通知列表
- 通知菜单
- 导航菜单
- 动态进度条
- 可配置型表单
- 可配置型表格
- 弹出框表单
- 日历

具体用法请参考一下文档说明，了解组件的具体使用方式。  
效果预览请点击右上角`github站点`。